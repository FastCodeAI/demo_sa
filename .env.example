# Copy to .env and fill in. The agentic layer (demo_scheduler chat / whatif)
# auto-loads .env at import. Either key works; OpenAI is preferred when both
# are set. With neither key, use MockLLM in tests.
OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
